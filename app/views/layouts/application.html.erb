<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Linkedin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%if session.include? :current_user %>
    <%# <button action="login#delete">Logout</button> %>
    <%# <%= link_to "Sign out", "login", method="delete" %> 
    <%= button_to "Logout" , {:controller => 'login', :action => 'delete'}, {:method => 'delete'}%>
    <%# <%= button_to "Add Skill" , {:controller => 'skill', :action => 'new'}, {:method => 'post'}%> 
    <%= link_to "Add New Skill", skills_path %>
    <%end%>
    <%= yield %>
  </body>
</html>
